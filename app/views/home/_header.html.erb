<nav class="navbar navbar-expand-lg bg-body-tertiary bg-dark border-bottom-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <!-- Root Button -->
    <%= link_to "Auth WebApp", root_path, class: "navbar-brand" %>

    <!-- Collapse Button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- NavBar Buttons -->
    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <!-- Left Side Buttons -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
<!--        <li class="nav-item">-->
<!--          <a class="nav-link" href="#">Link</a>-->
<!--        </li>-->
      </ul>

      <!-- Right Side Buttons -->
      <ul class="navbar-nav d-flex">
        <!-- Checking User Logged in the system -->
        <% if user_signed_in? %>
          <!-- Current User -->
          <li class="nav-item">
            <a class="nav-link disabled" href=""><%= current_user.email %></a>
          </li>

          <!-- Update Profile Button -->
          <li class="nav-item">
            <%= link_to "Profile Update", edit_user_registration_path, class: "nav-link" %>
          </li>

          <!-- Sign Out Button -->
          <li class="nav-item">
            <%= button_to "SignOut", destroy_user_session_path, method: :delete, class: "nav-link" %>
          </li>
        <% else %>
          <!-- Sign In Button -->
          <li class="nav-item">
            <%= link_to "SignIn", new_user_session_path, class: "nav-link" %>
          </li>

          <!-- Sign Up Button -->
          <li class="nav-item">
            <%= link_to "SignUp", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>